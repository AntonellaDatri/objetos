package ar.edu.unq.eperdemic.mutacionTests

import ar.edu.unq.eperdemic.modelo.Mutacion
import org.junit.jupiter.api.Test
import org.junit.Assert

/*
class MutacionTest {
    private val requerimientos : MutableList<Mutacion> = mutableListOf()
    private val mutacion = Mutacion("Fiebre", requerimientos, 6)
    @Test
    fun unaMutacionSabeResponderSuNombre() {
        Assert.assertEquals("Fiebre", mutacion.nombre)
    }

    @Test
    fun unaMutacionSabeResponderSuIdSiLeAsignoUno() {
        mutacion.id = 1
        Assert.assertEquals(1, mutacion.id!!.toInt())
    }

    @Test
    fun unaMutacionSabeResponderSusRequerimientos() {
        val requisitos : MutableList<Mutacion> = mutableListOf()
        Assert.assertEquals(requisitos, mutacion.getRequisitos())
    }

    @Test
    fun unaMutacionSabeResponderSusADNRequeridos() {
        Assert.assertEquals(6, mutacion.getpuntosDeADNRequeridos())
    }

    @Test
    fun unaMutacionSabeResponderLasMutacionesADesbloquear() {
        val aDesbloquear : MutableList<Mutacion> = mutableListOf()
        Assert.assertEquals(aDesbloquear, mutacion.getMutacionesADesbloquear())
    }

    @Test
    fun unaMutacionSabeAgregarUnRequerimiento() {
        val requisitos : MutableList<Mutacion> = mutableListOf()
        val tos = Mutacion("Tos", requerimientos, 2)
        requisitos.add(tos)
        mutacion.agregarRequisito(tos)
        Assert.assertEquals(1, mutacion.getRequisitos().size)
        Assert.assertEquals(requisitos, mutacion.getRequisitos())
    }
}*/